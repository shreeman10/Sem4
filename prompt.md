I have a project called AgriVision - an AI-powered plant disease detection web application. Currently it's built with React frontend, Django backend, and a Keras (.h5) deep learning model that classifies 38 plant diseases from leaf images.
I need you to help me transform this project with the following requirements:
MAIN REQUIREMENT 1: Convert Backend to Node.js/Express (Keep AI Model in Python)

Replace the entire Django backend with Node.js and Express
The Python-based AI inference script (inference.py) and Keras model (.h5 file) should remain in Python - do NOT convert them to JavaScript
The Node.js backend should call the Python inference script as a child process or through a separate microservice approach
When a user uploads an image, Node.js should save it, pass it to the Python script, get the JSON result back, and return it to the frontend
Keep all the AI logic (image preprocessing, prediction, confidence scoring, severity estimation, Grad-CAM heatmap generation) in Python exactly as it is
Update the React frontend API calls to work with the new Node.js/Express backend

MAIN REQUIREMENT 2: Build a Complete Admin Panel

Create a separate admin dashboard accessible only to administrators
Admin should be able to view all users, their diagnosis history, and system-wide statistics
Admin can manage users (view details, deactivate/activate accounts, delete users)
Admin can see platform analytics: total diagnoses, most common diseases, regional data if available, user growth charts
Admin should be able to view and manage all stored images and diagnosis records
Admin can configure application settings like API rate limits, file upload size limits
Admin panel should have its own React-based UI with proper routing
Admin panel should have its own authentication separate from regular users

MAIN REQUIREMENT 3: Implement Proper Authentication System

Build a secure JWT-based authentication system in Node.js/Express backend
NO client-side token exposure in localStorage or environment variables that users can see
Implement HTTP-only cookies for storing auth tokens
Add proper session management
Include password reset functionality via email
Implement role-based access control (Regular User vs Admin)
Admin credentials should be manageable through the admin panel itself (not hardcoded)
Add user registration with email verification
Implement password hashing using bcrypt
Add refresh token mechanism for security
Protect all API routes with proper middleware authentication

MAIN REQUIREMENT 4: Implement All Simple Changes Previously Discussed

Replace SQLite with MongoDB (since we're going full JavaScript stack for the backend)
Add proper error handling everywhere in both frontend and backend
Add loading states and progress indicators for all async operations
Store complete diagnosis history in database with user associations
Add image validation on both frontend and backend (file type, size limits)
Create comprehensive treatment recommendations for all 38 diseases in the database
Add timestamps to all diagnoses
Use environment variables properly with dotenv for all configuration
Build real-time analytics dashboard using actual database data (not mock data)
Make the application deployment-ready with proper configuration files

ADDITIONAL REQUIREMENTS:

Use modern Node.js practices (async/await, ES6+ syntax)
Implement proper folder structure for Node.js/Express backend with separate routes, controllers, models, and middleware
Add API documentation structure (prepare for Swagger/OpenAPI if needed)
Include proper CORS configuration
Add request validation middleware using express-validator or Joi
Implement rate limiting to prevent API abuse
Add file upload handling with multer for image uploads
Set up proper logging system (use Winston or Morgan)
Include database schema design for MongoDB using Mongoose
Ensure the Python script integration is robust with proper error handling
Add health check endpoints for monitoring

PYTHON-NODE.JS INTEGRATION CLARIFICATION:

The Node.js backend should seamlessly integrate with the existing Python AI model
You can use child_process to spawn Python scripts, or suggest a microservice architecture if better
Make sure image paths are properly passed between Node.js and Python
Handle any errors from Python script execution gracefully
Ensure the Grad-CAM heatmap images generated by Python are accessible to the frontend

IMPORTANT INSTRUCTION:
Before writing any code or making any changes, I need you to create a detailed roadmap document that explains:

How you will approach this entire transformation
What will be the new project structure (folders and files) for both frontend and backend
Step-by-step breakdown of the conversion process from Django to Node.js/Express
Which libraries and packages you'll use for each functionality (list npm packages)
How you'll handle the integration between Node.js backend and Python AI model (architecture diagram in text)
Database schema design for MongoDB with Mongoose models
Complete API endpoint structure for the Node.js/Express backend (list all routes)
Authentication flow architecture (how JWT, cookies, and sessions work together)
Admin panel architecture, routes, and components structure
How file uploads and image storage will work
Any potential challenges and how you'll solve them
Testing strategy for both backend and Python integration
Deployment considerations (how to deploy Node.js + Python together)
Environment variables and configuration management strategy

Please create this comprehensive roadmap first, get my approval, and only then proceed with the actual implementation. The roadmap should be detailed enough that another developer could follow it to rebuild the project from scratch.
The roadmap should clearly separate:

What stays in Python (AI model and inference logic)
What gets built in Node.js/Express (API, authentication, database operations)
What gets enhanced in React (frontend features, admin panel)
How these three parts communicate with each other